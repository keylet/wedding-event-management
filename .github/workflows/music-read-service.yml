name: Docker CI/CD for Read Music Service

on:
  push:
    paths:
      - 'music-service/read-music/**'
    branches: ["qa"]
  pull_request:
    paths:
      - 'music-service/read-music/**'
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: temurin

      - name: Build with Maven
        run: mvn clean package -DskipTests -f ./music-service/read-music/pom.xml

      - name: Rename JAR file
        run: |
          mv ./music-service/read-music/target/music-service-0.0.1.jar ./music-service/read-music/read-music-service.jar
  dockerize-and-deploy:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/read-music-service:latest ./music-service/read-music
          docker push ${{ secrets.DOCKER_USERNAME }}/read-music-service:latest
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.EC2_HOST_MUSIC }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script: |
              # Instalar Docker si no está instalado
              if ! command -v docker &> /dev/null; then
                echo "==> Installing Docker"
                sudo apt-get update -y
                sudo apt-get install -y docker.io
                sudo systemctl start docker
                sudo systemctl enable docker
                sudo usermod -a -G docker $(whoami)
              fi
              # Detener y eliminar cualquier contenedor en ejecución con el mismo nombre
              echo "Stopping and removing existing container..."
              sudo docker stop read-music-service || true
              sudo docker rm read-music-service || true
              # Eliminar imagen antigua
              echo "Removing old Docker image..."
              sudo docker rmi ${{ secrets.DOCKER_USERNAME }}/read-music-service:latest || true
              # Descargar nueva imagen
              echo "Pulling latest Docker image..."
              sudo docker pull ${{ secrets.DOCKER_USERNAME }}/read-music-service:latest
              # Ejecutar nuevo contenedor
              echo "Starting new container..."
              sudo docker run -d \
                --name read-music-service \
                -p 8062:8062 \
                -e SERVER_PORT=${{ secrets.SERVER_PORT_READ_MUSIC }} \
                -e EUREKA_CLIENT_SERVICE_URL=${{ secrets.EUREKA_CLIENT_SERVICE_URL }} \
                -e SPRING_DATASOURCE_URL=${{ secrets.SPRING_DATASOURCE_URL_MUSIC }} \
                -e SPRING_DATASOURCE_USERNAME=${{ secrets.SPRING_DATASOURCE_USERNAME_M }} \
                -e SPRING_DATASOURCE_PASSWORD=${{ secrets.SPRING_DATASOURCE_PASSWORD }} \
                --restart=always \
                pammec/read-music-service:latest
                